# Maverick-SORT v7 (Essential Edition) — Full Dependencies for Streamlit Cloud
# Now includes HuggingFace AI Copilot support (no more offline-only mode).
# Recommended Python: 3.12

streamlit==1.58.0
numpy==2.4.6
pandas==3.0.3
altair==6.1.0
plotly==6.8.0
streamlit-autorefresh==1.0.1
scipy==1.17.1
scikit-learn==1.8.0
pillow==12.2.0
matplotlib==3.10.0
tqdm==4.67.3

# ── AI / HuggingFace (Maverick Copilot) ──
# NOTE: torchvision is NOT listed here because it causes dependency conflicts
# with torch==2.12.0 and is only needed for local vision models. The code
# gracefully degrades when torchvision is absent (see hf_integration/config.py).
huggingface-hub==1.18.0
transformers==5.10.2
torch==2.12.0
sentence-transformers==5.5.1
